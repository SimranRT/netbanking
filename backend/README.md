# KodbBank Backend

Backend API for KodbBank application.

## Quick Start

1. Install dependencies: `npm install`
2. Copy `.env.example` to `.env` and configure your Aiven MySQL credentials
3. Initialize database: `node models/initDatabase.js`
4. Start server: `npm start` or `npm run dev`

## Environment Variables

See `.env.example` for required variables.
