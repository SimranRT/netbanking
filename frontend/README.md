# KodbBank Frontend

React frontend for KodbBank application.

## Quick Start

1. Install dependencies: `npm install`
2. (Optional) Copy `.env.example` to `.env` and configure API URL
3. Start dev server: `npm run dev`

## Build for Production

```bash
npm run build
```

The built files will be in the `dist` directory.
